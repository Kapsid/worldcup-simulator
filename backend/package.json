{
  "name": "worldcup-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "NODE_ENV=test mocha --recursive test/**/*.test.js --timeout 10000 --exit",
    "test:unit": "NODE_ENV=test mocha --recursive test/unit/**/*.test.js --timeout 5000 --exit",
    "test:integration": "NODE_ENV=test mocha --recursive test/integration/**/*.test.js --timeout 10000 --exit",
    "test:coverage": "NODE_ENV=test c8 npm test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^8.0.0",
    "simple-update-notifier": "^2.0.0"
  },
  "devDependencies": {
    "c8": "^8.0.1",
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "mongodb-memory-server": "^9.1.3",
    "nodemon": "^3.0.0",
    "sinon": "^17.0.1",
    "supertest": "^6.3.3"
  }
}
